import fs from "fs";

// Insert image data to the server
export function uploadLessonImage(request, response) {
    const receivedData = request.body;
    console.log(receivedData);
    // image.mv("./uploads/images/users/" + image.name);
}
